(executables
 ((names (caldav_server))
  (libraries (caldav webmachine nocrypto lwt ptime ptime.clock.os cohttp-lwt-unix rresult))))

(install
 ((section bin)
  (files ((caldav_server.exe as caldav_server)))))
