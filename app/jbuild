(executables
 ((names (caldav_server))
  (libraries (logs logs.fmt caldav webmachine nocrypto lwt ptime ptime.clock.os cohttp-lwt-unix mirage-fs-unix rresult))))

(install
 ((section bin)
  (files ((caldav_server.exe as caldav_server)))))
