(jbuild_version 1)

(library
 ((name        caldav)
  (public_name caldav)
  (preprocess (pps (ppx_deriving.std)))
  (libraries (lwt ptime ptime.clock.os cohttp mirage-fs-unix mirage-fs-mem xmlm tyxml icalendar rresult))))
